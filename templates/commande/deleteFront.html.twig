
{% block AabiElWest1%}
    <form method="post" action="{{ path('commande_deletefront', {'id': commande.idCommande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.idCommande) }}">
        <button class="btn">Supprimer</button>
    </form>
{% endblock %}
