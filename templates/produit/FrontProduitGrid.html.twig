{% extends 'baseF.html.twig' %}


{% block stylesheets %}
    {{ parent() }} {# Inclut le contenu du bloc stylesheets du template parent #}

    <style>
        .wrapper {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            grid-gap: 30px;
        }
        .product-item {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease-in-out;
            margin-left: 20px;
            margin-right: 20px;
        }
        .product-item a {
            display: inline-block;
            background-color: #ff69b4;
            padding: 10px 20px;
            border-radius: 20px;
            color: white;
            text-decoration: none;
        }
        .product-item:hover {
            transform: translateY(-5px);
        }
        .product-item img {
            max-width: 125%;
            height: 125%;
        }
         .product-price {
            font-size: 24px;
            color: #333;
            margin-bottom: 15px;
        }
        .product-item a {
            display: inline-block;
            background-color: #ff69b4;
            padding: 10px 20px;
            border-radius: 20px;
            color: white;
            text-decoration: none;
        }
        .background-colour {
    background-color: #f5f5f5; /* Remplacez cette valeur par la nuance de gris souhaitée */
    /*background-image: url('images/ringi.jpg');*/
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    height: 100vh;
    position: relative;
}
 .add-to-cart {
            background-color: #ff69b4;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 20px;
            cursor: pointer;
        }
        .add-to-cart:hover {
            background-color: #ff3385;
        }
        .spinner {
            border: 4px solid rgba(0, 0, 0, 0.1);
            border-top: 4px solid #ff69b4;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

    </style>
{% endblock %}


 {% block AabiElWest1 %}
<div class="fashion_section">
<div class="background-colour">
<div class="wrapper">
{% for product in prod %}
        <div class="product-item ">
           <img src="{{ asset('uploadsProduit/' ~ product.ImageP) }}" alt="Image du produit"  style="max-width: 100px; max-height: 100px;">
            <h3>{{ product.NomP }}</h3>
            <p>Prix : {{ product.PrixP }} DT</p>
          
            <a class="product-button" href="{{ path('product_details', {'idp': product.Id_Produit}) }}">Consulter</a> 
            <!-- Ajoutez ici le spinner pour la quantité -->
            <!-- Ajoutez ici le bouton "Ajouter au panier" -->

        </div>
    {% endfor %}



</div>




 {% endblock %}



 